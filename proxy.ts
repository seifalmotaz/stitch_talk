import { clerkMiddleware } from "@clerk/nextjs/server";

// Authentication is enforced where protected data is read. Proxy keeps Clerk's
// session state available to every matched request without relying on routes.
export default clerkMiddleware();

export const config = {
  matcher: [
    "/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)",
    "/(api|trpc)(.*)",
    "/__clerk/:path*",
  ],
};
